import Selector from "../../components/Selector";

const ChateauSelectors = () => {
  return (
    <Selector header={"Nothing"}>
      <option value="1">What's up?</option>
    </Selector>
  );
};

export default ChateauSelectors;
